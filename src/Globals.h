#pragma once
#include <glm/glm.hpp>

namespace Globals{
    extern int numballs;
    extern int numlights;
    extern float sensitivity;
    extern int windowsizex;
    extern int windowsizey;
    extern glm::vec3 accel;
    extern bool twoD;

    extern glm::vec3 camCenter;
    extern glm::vec3 camView;
    extern glm::vec3 camUp;
    extern bool shading;

}
